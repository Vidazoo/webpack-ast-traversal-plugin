{
  "name": "root",
  "private": true,
  "scripts": {
    "build": "lerna run build --parallel",
    "build-v3": "lerna run build --scope=v3 --stream",
    "build-v4": "lerna run build --scope=v4 --stream",
    "test": "lerna run test --stream"
  },
  "devDependencies": {
    "lerna": "3.15.0"
  }
}
